/** @deprecated Import from `./storekit-bridge` — kept for older docs. */
export {
  purchaseComplete,
  restoreComplete,
  getCompleteActive,
  PRODUCT_IDS,
  PRODUCT_PRICE_STATUS,
  STOREKIT_RULES,
  type CompletePlan,
  type PurchaseResult,
  type RestoreResult,
} from './storekit-bridge';
